<h1 class= 'welcome_title' >Welcome </h1>
<ul>
  <% @users.each do |user| %>
  <% p user %>
    <li> <%= link_to user.first_name, user_path(user) %> 
        <%= link_to "delete", user,  method: :delete, data: {confirm: 'Are you sure?'} %>
    </li>
    <% end %>
</ul>
<%= link_to 'New', new_user_path %>

